# Maint-Loop
An autonomous, decoupled agent engine built with a Vue 3 PWA dashboard and a Node.js/MongoDB backend. Executes sequential data feed ingestion, state-tracking routines via tool-calling LLMs, and features a context-aware retrieval architecture for real-time operation monitoring.
